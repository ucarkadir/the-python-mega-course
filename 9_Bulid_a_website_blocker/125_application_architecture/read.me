python dosyanızın uzantısını 
    pxw
olarak değiştirdiğiniz zaman scheduling program oluşturmuş 
olacbilirsiniz. 

Windows > 
    Task Scheduler 

yardımı ile de bu programı zamanlayabilir siniz. 


Linux:

    hosts_path = "/etc/hosts"
    
    Terminal: dosyayı açmak için
    sudo nano /etc/hosts 

    sudo crontab -e 